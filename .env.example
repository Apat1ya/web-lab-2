PGHOST=localhost
PGPORT=5432
PGDATABASE=bookstore
PGUSER=postgres
PGPASSWORD=postgres
PORT=3000

# Alternatively, set DATABASE_URL instead of the PG* values above:
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/bookstore
